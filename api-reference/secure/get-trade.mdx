---
title: "Get Self-Trade(s)"
description: "Fetch your trades by internal trade id or by the externalId you supplied at creation, one or up to 100 at a time. Covers self-trades and client trades alike."
---

Returns the calling merchant's trades of any source: self-trades from `/secure/sell`, `/secure/buy` and `/secure/buy/quick` (`source: "self"`) as well as trades your clients created (`source: "client"`). `tradeId` accepts either the internal trade id or the `externalId` you supplied at creation. Another merchant's trade is treated as not found.

**Authentication:** Merchant API Key (`api-key` header)
**Scope:** `CORE_ACCESS`

## Request (single)

```http
GET https://api.assetpay.gg/secure/trades/wd_unique_789
api-key: ap_...
```

A single ref returns one [`Trade`](/reference/types#trade) object, or `404 TRADE_NOT_FOUND` if it doesn't resolve. Duplicate refs are merged first, so `wd_1,wd_1` counts as a single ref: it returns one object and can 404.

```json
{
  "requestId": "...",
  "success": true,
  "data": {
    "id": "trade-uuid",
    "type": "withdraw",
    "source": "self",
    "status": "hold",
    "game": "730",
    "externalId": "wd_unique_789",
    "merchantId": "merchant-uuid",
    "clientSteamID": "76561198012345678",
    "clientTradeUrl": "https://steamcommunity.com/tradeoffer/new/?partner=12345&token=ABCDEFGH",
    "items": [ /* InventoryItem[] */ ],
    "totalPrice": 45.00,
    "offerID": "9088123456",
    "botInfo": {
      "name": "tradingbot01",
      "avatar": "https://...",
      "steamId": "76561198..."
    },
    "createdAt": "2026-05-14T12:00:00.000Z",
    "updatedAt": "2026-05-14T12:01:30.000Z"
  }
}
```

## Fetching multiple (batch)

Comma-separate up to **100** ids/externalIds to fetch several in one call, which suits polling the status of multiple pending withdrawals without one request per trade.

```http
GET https://api.assetpay.gg/secure/trades/wd_1,wd_2,wd_3
api-key: ap_...
```

With more than one ref the `data` is an **array** of the trades that resolved. Unresolved refs are simply omitted (no `404`), so compare the returned `externalId`s against what you asked for to find the gaps.

```json
{
  "requestId": "...",
  "success": true,
  "data": [
    { "id": "...", "externalId": "wd_1", "status": "completed", "...": "..." },
    { "id": "...", "externalId": "wd_2", "status": "hold", "...": "..." }
  ]
}
```

(`wd_3` didn't resolve, so it's absent from the array.)

## Rate Limits

| Merchant Status | Limit |
|----------------|-------|
| Verified | 3,000 requests / min |
| Unverified | 100 requests / min |

## Errors

| Code | Key | When |
|------|-----|------|
| 21 | `TRADE_NOT_FOUND` | A single ref doesn't resolve (not found, or another merchant's trade). Batch requests omit unresolved refs instead of erroring. |
| 1001 | `VALIDATION_FAILED` | More than 100 distinct refs supplied in one request. HTTP 400. |
